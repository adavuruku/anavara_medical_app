# from rest_framework.routers import DefaultRouter

# from authentication.urls import router as census_data_router

# router = DefaultRouter()
# router.registry.extend([
#     *users_router.registry,
# ])
